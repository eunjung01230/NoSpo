# NoSpo

“다 본 사람 말고, 나만큼 본 사람들과.” 작성자가 지정한 글의 내용 범위와 독자의
감상 진도를 비교해 감상 글을 공개하는 웹 커뮤니티. AI 스포일러 판별 서비스가 아니다.

## 스택
Next.js(App Router, TypeScript) + Neon Postgres(`@neondatabase/serverless`).
연결 문자열은 `.env.local`의 `DATABASE_URL`(gitignore 대상, 출력·로그 금지).
스키마와 시연 데이터: `npm run db:setup` (재실행해도 기존 데이터를 덮어쓰지 않음).

## 핵심 규칙
- 공개 조건: `글의 max_stage <= 현재 사용자의 진도`. 판정은 항상 서버에서 한다.
- 진도 제한이 없는 게시판은 없다. 자유 게시판(`board_type = free`)의 '자유'는 이야기
  주제의 자유를 뜻하며, 공개 범위는 다른 게시판과 똑같다. 작성 화면에서 회차를 묻고,
  `max_stage <= 읽는 사람의 진도`로만 목록·상세·댓글을 연다. 게시판별 예외 분기를 만들지 않는다.
- 게시판(감상/질문/해석/후기/자유)은 `posts.board_type` 한 컬럼으로 구분한다. 게시판별 테이블을
  만들지 않으며, 게시판·내 글 필터는 공개 조건에 AND로만 덧붙인다.
- 화면에서는 게시판이 각각 독립된 공간으로 보이게 한다: 고유 주소
  (/works/[workId]/impressions|questions|analysis|reviews|lounge), 고유 제목·설명, 목록,
  게시판별 글쓰기 버튼. 슬러그와 타입 매핑은 lib/boards.ts에만 둔다.
- 수정·삭제는 `author_id = 현재 사용자`를 SQL 조건으로 강제하고, 0행이면 권한 없음으로 본다.
- 진도는 올리기와 낮추기 모두 가능하다. 낮추면 그 이후 글이 즉시 다시 숨겨지고 글은 남는다.
- 진도 단위는 works.progress_unit으로 구분한다: episode(화), film(편), volume(권),
  single(단일 작품, 단계 1개 "봤다"). 단일 영화를 회차로 억지 분할하지 않는다.
- 작품 데이터는 저작권 보호를 위해 작품명·포맷·진도 단위·한 줄 소개만 저장한다.
- 작품은 여러 개를 지원한다. 회차 표기는 `work_stages.label`과 `works.progress_unit`에서
  가져오고 화면에 "N화"를 하드코딩하지 않는다.
- 제한된 글의 제목·본문·미리보기는 쿼리 단계에서 제외한다. CSS나 화면 조건문으로
  숨기지 않고, 목록·상세·서버 렌더링 결과 어디에도 포함하지 않는다.
- 현재 사용자는 httpOnly 쿠키 id를 DB와 대조해 서버에서 확정한다. 모든 응답은
  `Cache-Control: no-store, private` + `Vary: Cookie` (next.config.ts).
- 사용자 전환은 공개 시연 장치이며 실제 인증이 아니다. 화면에 '시연 사용자'로 표시한다.
- 진도는 `사용자 + 작품` 조합으로 저장한다. 다른 작품·시리즈와 자동 연동하지 않는다.
- 작성 시 묻는 값은 '글에 포함된 마지막 회차'이며 작성자의 전체 진도와 다르다.
  이번 구현에서는 자기 진도 이하만 선택 가능(서버 검증).
- 빈 제목·본문, 존재하지 않는 회차 등 입력 오류는 서버에서도 검증한다.
- 댓글은 쓸 때 작성자의 그때 진도를 `comments.author_progress`에 함께 남긴다. 글이 열려
  있어도 `작성 당시 작성자의 진도 <= 읽는 사람의 지금 진도`가 아니면 본문을 쿼리에서
  빼고(select에서 null) 자리만 남긴다. 글쓴이 본인에게도 같은 규칙을 적용하며, 값이 없는
  예전 댓글은 그 글의 max_stage로 메운다.
- 댓글 입력칸 위에는 이 글이 어느 회차까지의 자리인지 한 줄로 밝힌다
  (lib/boards.ts의 commentScopeNote). 같은 말을 placeholder에서 반복하지 않는다.
- 댓글은 모든 게시판에 있고, 부르는 말만 다르다(질문 게시판은 "답글" — COMMENT_NOUN).
  댓글을 읽고 쓸 권한은 "그 글이 지금 나에게 공개되는가"와 같으며, 매번 서버에서 다시
  판정한다. 삭제는 `author_id = 현재 사용자`를 SQL 조건으로 강제한다.
- 답글(comments.parent_id)은 한 단계까지만 달린다. 답글에 또 답글을 달 수 없고,
  부모 댓글이 지워지면 답글도 함께 사라진다(on delete cascade).
- 감상 소요 시간은 `works.minutes_per_stage`(한 회차·편·권 평균 분) 하나로만 저장하고
  총·남은 시간은 진도와 곱해서 계산한다. 표시 문구는 lib/runtime.ts에만 둔다.
  시간은 표시·추천용일 뿐 공개 판정에 관여하지 않는다.
- 추천은 내가 글을 쓴 작품의 분야·장르만 보고(글 내용은 읽지 않는다) 아직 다 보지 않은
  작품을 남은 시간이 적은 순으로 고른다. AI 추천이 아니라 작품 메타 기반 정렬이다.
  노출 자리는 홈과 "작품을 끝까지 본 작품 화면" 두 곳이며 같은 컴포넌트를 쓴다.
- 시연 데이터는 `is_demo_seed`로 표시하고 실제 사건·결말을 지어내지 않는다.

## 나중에 로그인 추가할 자리
신원 확인은 `lib/session.ts`의 `getCurrentUser()` 한 곳에서만 결정하고, 데이터 함수는
모두 `userId`를 인자로 받는다. 실제 로그인을 붙일 때는 이 함수의 구현(쿠키 → 세션)만
교체하면 되고 화면·쿼리는 그대로 둔다. `users` 테이블도 그대로 쓰고 인증 식별자 컬럼만 더한다.

## 신고·경고·관리자 (공개 판정 위에 얹는 안전장치)
공개 판정은 여전히 `글의 max_stage <= 읽는 사람의 진도` 하나뿐이고, 아래 장치들은 그 위에만
더한다. 판정 규칙 자체를 우회하거나 바꾸는 예외를 만들지 않는다.
- AI 사전 검토(lib/moderation.ts)는 '이 글이 작성자가 고른 범위를 넘는가'만 본다.
  결과는 경고까지이고 등록을 막지 않는다 — 회차를 고칠지 그대로 올릴지는 작성자가 정한다.
  그대로 올린 글은 `posts.ai_verdict = 'warn'`으로 남아 관리자 화면의 '검토 필요'에 쌓인다.
  Claude API(`claude-opus-5`, ANTHROPIC_API_KEY)를 쓰고, 키가 없거나 호출이 실패하면
  규칙 기반 검사로 내려간다. 판별이 실패했다고 글쓰기를 막지 않는다.
- 신고는 그 글이 지금 나에게 공개될 때만 할 수 있고(읽을 수 없는 글은 신고도 못 한다),
  내 글은 신고할 수 없다. 한 사람당 한 글에 1건이다(unique).
- 신고가 REPORT_HIDE_THRESHOLD건 모이면 글을 `posts.hidden_at`으로 가리고 작성자에게
  경고를 1건 남긴다. 가려진 글은 목록·상세·개수 어디에도 나오지 않으며(쿼리에서 제외),
  글 자체는 지우지 않는다. 관리자가 숨김을 풀면 그 글로 생긴 경고도 함께 사라진다.
- 경고가 WARNING_BLOCK_THRESHOLD건 쌓인 사용자는 새 글·수정이 막힌다(서버에서 판정).
- 관리자는 `users.is_admin`으로만 구분하고, 화면(/admin)과 모든 관리자 액션에서
  `requireAdmin()`으로 다시 확인한다. 신고를 판단하려면 글을 읽어야 하므로 관리자만
  가려진 글을 열람할 수 있으며, 그 화면에는 '관리자 열람'이라고 밝힌다.
- 문턱값·신고 사유 문구는 lib/moderation.ts 한 곳에만 둔다.

## 환경 변수
Vercel 프로젝트 `nospo`에 Neon 통합으로 `DATABASE_URL` 등이 Preview/Production에
등록되어 있다. Secret이라 `vercel env pull`로는 값을 내려받을 수 없으므로, 로컬은 Neon
콘솔의 연결 문자열을 `.env.local`에 직접 입력한다. 연결 확인은 `npm run db:check`.
AI 사전 검토를 실제로 쓰려면 `ANTHROPIC_API_KEY`가 로컬(.env.local)과 Vercel 양쪽에
있어야 한다. 없으면 규칙 기반 검사로만 동작한다(기능은 그대로 돌아간다).

## 작품 메타
포스터 이미지는 저작권이 있으므로 파일을 저장하지 않고, 공개 API가 주는 주소만
works.poster_url에 담아 불러온다(영화·드라마·애니는 TMDB — TMDB_API_KEY 필요,
책·만화는 Open Library). 채우는 스크립트는 npm run posters이며, 주소가 없으면 같은
비율의 자리표시자가 나온다. 감상 시간(minutes_per_stage)은 npm run runtimes로 TMDB에서
받아온다(드라마·애니는 회차 평균, 단일 영화는 러닝타임, 영화 시리즈는 편들의 평균).
책·만화는 공개 API에 읽는 시간이 없어 db:setup의 기본값을 그대로 둔다. 두 스크립트가
채운 값은 db:setup을 다시 돌려도 덮어쓰지 않는다. 줄거리 전문·대사는 저장하지 않고 한 문단 소개와 연도·제작자만 둔다.

## 검색과 작품 추가
- 검색(/search, 헤더의 돋보기)은 **작품만** 찾는다. 대상은 works의 제목·제작자·한 줄 소개뿐이고
  글은 검색하지 않는다. 그래서 아직 열리지 않은 글의 제목이 검색으로 새어 나갈 길이 없다.
  글까지 찾게 만들 때는 공개 조건을 SQL에 그대로 AND로 붙여야 한다.
- 작품 추가(/works/new)는 시연 사용자 누구나 할 수 있다. 받는 값은 저작권 규칙 그대로
  작품명·분야·국내외·장르·진도 단위·회차 수·한 줄 소개·한 회차 감상 시간까지다.
  포스터 주소·연도·제작자는 등록할 때 TMDB(영화·드라마·애니)와 Open Library(만화·책)에서
  한 번 찾아 채우며(lib/artwork-lookup.ts), 키가 없거나 조회가 실패해도 등록은 그대로 된다.
- 회차는 작품 등록과 한 묶음으로 만든다(1화·1편·1권…). 단일 작품은 단계 하나("봤다")뿐이다.
- 포스터·연도·제작자는 **자동으로 붙이지 않고 후보를 보여주고 고르게 한다**. 공개 API는 제목이
  글자 단위로 맞아야 찾아주기 때문에('뷰티인사이드' 0건, '뷰티 인사이드' 적중) 질의가 비면
  띄어쓰기를 지우고 앞부분만 남기는 식으로 느슨하게 다시 찾는다. 고르지 않아도 등록은 된다.
- 작품 수정·삭제(/works/[workId]/edit)는 등록한 사람과 관리자만 할 수 있고 권한은 canEditWork로
  서버에서 다시 본다. 진도 단위는 등록 뒤 바꾸지 않고, 회차는 늘릴 수 있으며 줄이는 것은 그 뒤
  회차에 글·진도가 없을 때만 받는다. 글이 하나라도 있는 작품은 삭제하지 않는다.
- 같은 제목이 이미 있으면 등록하지 않고 기존 작품으로 안내한다. 사용자가 등록한 작품은
  works.created_by로 구분하며, 이 값은 표시용일 뿐 공개 판정에는 관여하지 않는다.

## 카테고리
작품은 분야(works.category: movie/drama/anime/comic/book), 국내외(works.origin:
domestic/foreign, 영화·드라마만), 장르(works.genre)로 분류한다. 탐색은 왼쪽 CATEGORIES 사이드바(app/CategorySidebar.tsx)로 하며, 현재 분야 아래에
국내·외국과 장르가 펼쳐진다. 경로는
/categories/[category] → (영화·드라마는 [origin] →) [genre] 순이며, 슬러그 매핑과
분야 목록은 lib/categories.ts 한 곳에만 둔다. 분류는 작품 메타일 뿐이고 글 공개 판정에는
관여하지 않는다.

## 디자인
밤의 서재 톤(웜 다크 + 종이). 토큰은 app/globals.css의 --ns-* 한 곳에서만 정의한다:
어두운 층(배경 #27221F → 섹션 #342D29 → 패널 #3A322D)은 탐색·진도, 밝은 종이 층
(#E7DED2 / 작성 #EEE7DC / 입력 #FBF8F3)은 읽고 쓰는 영역이다. 와인색 #7A4E4E와
말린 장미 #C08A7E는 회차·진도·주요 행동에만 쓴다. 서체는 제목·진도 Gowun Batang,
본문 Pretendard, 숫자·라벨 IBM Plex Mono. 반경은 2·4와 세그먼트 pill만, 그림자는 쓰지 않는다.
게시판은 같은 종이 시트 안에서 목록 형식이 달라진다(감상 행 / 질문 Q. / 해석 넓은 행 / 후기 카드).

## 이번 범위 밖
좋아요, 알림, 잠금 카드 UI,
댓글 수정, 두 단계 이상의 대댓글, 작품과 무관한 전체 자유 게시판.
미정: 서비스명·최종 디자인, 잠금 카드 표시 방식.
